// automatically created from Version.h.cmake

#include <digi/Base/VersionInfo.h>

#cmakedefine NAME              "${NAME}"
#cmakedefine REVISION          "${REVISION}"

static digi::VersionInfo versionInfo = {NAME, REVISION, NULL};
