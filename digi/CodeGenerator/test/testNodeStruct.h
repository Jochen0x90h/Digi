struct T1Local
{
	struct
	{
		struct
		{
			struct
			{
				float _x;
				float _y;
			} _bar[4u];
		} _foo;
		float _output;
		struct
		{
			float _bar;
		} _subNode;
	} _node;
};

// init
{
}

// update
{
	
	// 'graph.node' (Node)
	{
	}
	
	// 'graph.node.subNode' (Node)
	{
	}
}
